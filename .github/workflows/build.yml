on:
  push:
    branches: ["main"]
  pull_request: ~

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: astral-sh/setup-uv@v5
    - name: test
      run:
        uv run pytest
